name: 🚀 Auto Publish Clean Architecture Template

on:
  push:
    branches:
      - main
  workflow_dispatch:  # também permite rodar manualmente no GitHub

permissions:
  contents: write
  packages: write

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # necessário para ler tags anteriores

      - name: 🧩 Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 9.0.x

      - name: 🏷️ Detect next semantic version
        id: versioning
        run: |
          echo "🔍 Checking last Git tag..."
          last_tag=$(git describe --tags --abbrev=0 2>/dev/null || echo "v1.0.0")
          echo "Last tag: $last_tag"
          version=${last_tag#v}
          IFS='.' read -r major minor patch <<< "$version"
          patch=$((patch + 1))
          new_version="$major.$minor.$patch"
          echo "Next version: v$new_version"
          echo "VERSION=$new_version" >> $GITHUB_ENV

      - name: 🏗️ Build and pack NuGet template
        run: |
          dotnet pack ./GRRInnovations.CleanArchitecture.Template.csproj \
            --configuration Release \
            --output ./nupkgs \
            /p:Version=${{ env.VERSION }}
          echo "✅ Template version ${{ env.VERSION }} packed successfully."
          ls ./nupkgs

      - name: 🏷️ Create and push new Git tag
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag "v${{ env.VERSION }}"
          git push origin "v${{ env.VERSION }}"
          echo "✅ Tag v${{ env.VERSION }} pushed to repository."

      - name: 🚀 Publish to NuGet.org
        env:
          NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
        run: |
          echo "Pushing package to NuGet.org..."
          dotnet nuget push ./nupkgs/*.nupkg \
            --api-key $NUGET_API_KEY \
            --source https://api.nuget.org/v3/index.json \
            --skip-duplicate
          echo "🎉 Package v${{ env.VERSION }} published to NuGet.org!"
